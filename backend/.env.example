# Cloudflare Workers 환경 변수 예제

# R2 스토리지 설정
# wrangler.toml에서 바인딩으로 설정되므로 실제로는 필요하지 않음
# STORAGE_BUCKET_NAME=converter-files

# 파일 크기 및 보존 정책
MAX_FILE_SIZE=104857600
FILE_RETENTION_HOURS=24

# CORS 설정
ALLOWED_ORIGINS=https://converter.hqmx.net,http://localhost:3000

# API 보안 (옵션)
# API_KEY=your-secret-api-key

# 로그 레벨
LOG_LEVEL=info

# 변환 도구 설정 (Cloudflare Workers에서는 사용하지 않음)
# FFMPEG_PATH=/usr/bin/ffmpeg
# IMAGEMAGICK_PATH=/usr/bin/convert
# LIBREOFFICE_PATH=/usr/bin/libreoffice